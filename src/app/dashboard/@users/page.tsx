import { NextPage } from "next";

interface Props {}

const UserAnalytic: NextPage<Props> = ({}) => {
  return (
    <div className="p-4 bg-blue-900">
      <h1>user analytic parallel route</h1>
    </div>
  );
};

export default UserAnalytic;
