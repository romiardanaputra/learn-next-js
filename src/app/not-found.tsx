import { NextPage } from "next";

interface Props {}

const NotFound: NextPage<Props> = ({}) => {
  return (
    <div>
      <h2>Notfound Custom Page</h2>
    </div>
  );
};

export default NotFound;
