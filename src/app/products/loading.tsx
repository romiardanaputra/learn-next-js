import { NextPage } from "next";

interface Props {}

const ProductLoading: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>loading product...</h1>
    </div>
  );
};

export default ProductLoading;
